# connected-evolved--feature-connect-game

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build connected-evolved--feature-connect-game` to build the library.

## Running unit tests

Run `nx test connected-evolved--feature-connect-game` to execute the unit tests via [Jest](https://jestjs.io).
